const Button = (props) => {
    const name = props.title;
    return (
         <button className="flex items-center text-white bg-red-600 p-2 rounded-md"><span>Get Appointment</span></button>
    )
}

export default Button